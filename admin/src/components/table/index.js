import styled from "styled-components";

const TR = styled.tr``;
const TH = styled.th`
  text-align: left;
  padding: 8px;
`;
const TD = styled.td`
  padding: 8px;
`;
const Body = styled.tbody``;
const Header = styled.thead``;

export const Table = { TR, TH, TD, Body, Header };
